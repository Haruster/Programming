# list.insert : 원소에 자료를 추가한다. 
#   -> list.insert(i, d) : 인덱스 i뒤에 자료 d를 추가(i는 위치, d는 값이다.) (오직 한 개의 자료만 넣을 수 있다.)

c = [1, 2, 4, 5]

c.insert(2, 3) #위치 2뒤에 값 3을 넣는다.

print(c) #['1','2','3','4','5'] 가 출력된다.