# 시퀀스 자료형 : 순서가 있는 자료형으로 리스트, 문자열 등이 이에 속한다.
    # 원소간에 순서가 존재하여 -> 인덱싱 / 슬라이싱이 가능하다.
a = "Once" #문자열

b = ['T', 'W', 'I', 'C', 'E'] #리스트

c = (1, 2, 3, 4, 5) #튜플

print(a[1]) #n이 출력됨
print(b[2:4]) #['I', 'C']가 출력됨. 

